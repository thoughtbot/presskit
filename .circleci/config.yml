version: 2

jobs:
  build:
    docker:
      - image: circleci/ruby:2.6.4-node

    steps:
      - checkout

      - restore_cache:
          keys:
            - presskit-bundle-v2-{{ checksum "Gemfile.lock" }}
            - presskit-bundle-v2-

      - run:
          name: Install gem dependencies
          command: bundle install --path vendor/bundle

      - save_cache:
          key: presskit-bundle-v2-{{ checksum "Gemfile.lock" }}
          paths:
            - vendor/bundle

      - restore_cache:
          keys:
            - presskit-npm-v2-{{ checksum "package-lock.json" }}
            - presskit-npm-v2-

      - run:
          name: Install npm dependencies
          command: npm install

      - save_cache:
          key: presskit-npm-v2-{{ checksum "package-lock.json" }}
          paths:
            - ~/usr/local/lib/node_modules

      - run:
          name: Build the Middleman site
          command: bin/build

      - run:
          name: Run HTMLProofer
          command: bin/test
